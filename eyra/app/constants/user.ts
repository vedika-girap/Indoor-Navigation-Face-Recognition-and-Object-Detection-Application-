/**
 * User Constants
 * 
 * Centralized user configuration to ensure consistency across the app
 */

/**
 * Demo User ID
 * 
 * This is used throughout the app for floor map operations.
 * In production, this should be replaced with actual user authentication.
 * 
 * IMPORTANT: Keep this consistent across all screens that use floor maps!
 */
export const DEMO_USER_ID = 'demo_user_123';

// Default export placeholder to satisfy Expo Router (not a route)
export default function UserConstantsPlaceholder(): null {
	return null;
}
